{% extends "base.twig" %}

{% block content %}
  {% if not isAJAX %}<div id="page-contact" class="page page-contact">{% endif %}
    <h1>{{ post.title }}</h1>
  {% if not isAJAX %}</div>{% endif %}
{% endblock %}
